#include<iostream>
#include<map>
#include<functional>
#include<algorithm>
#include<string>
#include<queue>
using namespace std;

/*
int main()
{
//关键字与语法
	//auto：可以根据右值，推导出右值的类型，然后左边变量的类型也就已知了
	//nullptr：给指针专用（与整数 NULL(0) 进行区分） #define NULL 0
	//foreach：可以遍历数组，容器等
		//for(Type val : container) 底层通过指针或迭代器来实现
			//{
			//	cout << val << " ";
			//}
	//右值引用：move 移动语义函数和 forward 类型完美转发函数
	//模板的一个新特性：typename... A 表示可变参（类型参数）

//绑定器和函数对象
	//function：函数对象
	//bind：绑定器，bindlst 和 bind2nd + 二元函数对象 = 一元函数对象
	//lambda 表达式
//智能指针
	//shared_ptr 和 weak_ptr

//容器
	//set 和 map：红黑树 0(lgn)
	//unordered_set 和 unordered_map：哈希表 0(1)
	//数组：array（不可扩容）、vector
	//链表：forward_list（前向链表）、list（双向链表）

//C++ 语言级别支持的多线程编程：支持跨平台
	//C++ 语言层面 thread 可以在编译的时候自动识别当前的操作系统来适配
	//即最后还是去调用 windows/linux 的 createThread/pthread_create
	//window:createThread
	//linux:pthread_create,clone

	system("pause");
	return 0;
}
*/